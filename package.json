{
  "name": "rivo-chat-cloudflare",
  "version": "156.0.0",
  "private": true,
  "description": "Rivo Chat v156: new desktop UI merged with the live Cloudflare Worker, Google authentication, rooms, moderation and radio backend.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
